#include <limits.h>
#include "gtest/gtest.h"
#include "MThread/MThread.h"
#include "File.h"

//MultiThread Test

TEST(MultiThreadTest, Load) {
  
  File of("../File/test.txt");
  /*
  MThread mt(( MThread(of_) ));
  mt.load(); // split data into 27 cubes
    mt.print();
    */
    
  of.parseText(false);
  EXPECT_EQ(27, of.getTotalLines());

}

TEST(MultiThreadTest, GetCube) {
  
}

TEST(MultiThreadTest, GetCubeNumber) {

}

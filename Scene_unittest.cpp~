#include <limits.h>
#include "Scene.h"
#include "gtest/gtest.h"

//Parsing Test

TEST(IsRandomPointTest, LimiteValues) {
	Scene sc("../File/star.txt");
	EXPECT_EQ(true, sc.isRandomPoint(100));
	EXPECT_EQ(false, sc.isRandomPoint(0));
}
